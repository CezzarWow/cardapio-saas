{
  "name": "cardapio-saas-assets",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "clean-css": "^5.3.2",
    "esbuild": "^0.19.0",
    "esbuild-sass-plugin": "^2.4.0",
    "tailwindcss": "^4.1.18"
  },
  "scripts": {
    "build": "node build-assets.js && npm run build:css",
    "build:css": "tailwindcss -i ./public/css/tailwind-input.css -o ./public/dist/tailwind.css --minify"
  }
}